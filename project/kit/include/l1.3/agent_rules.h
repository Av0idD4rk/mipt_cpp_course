// Доступ к таблице правил агента. Занятие 1.3.
//
// Сами правила пишете вы: пять функций в src/agent_rules.cpp и constexpr-
// таблица, которая связывает идентификатор, важность и функцию-проверку.
// Какие это правила и по какому условию каждое срабатывает — в постановке
// занятия, раздел «Пять правил».
//
// Здесь объявлены только две функции, потому что таблица нужна не одному
// вашему main: через них до неё добираются выданные тесты. Где лежит ваша
// таблица и как называются ваши правила, они не знают и знать не должны.

#ifndef NANO_EDR_KIT_AGENT_RULES_H
#define NANO_EDR_KIT_AGENT_RULES_H

#include "rules.h"

namespace nano_edr {

// Таблица правил агента и её длина — ровно то, что принимает CheckRules.
const Rule* AgentRules();
size_t AgentRuleCount();

}  // namespace nano_edr

#endif  // NANO_EDR_KIT_AGENT_RULES_H
